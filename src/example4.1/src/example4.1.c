#include <stdio.h>
#include <stdlib.h>

double aax1(void);

int main() {
  double return_v, aax1(void);
  return_v = aax1();
  exit(EXIT_SUCCESS);
}

double aax1(void) {
  /* code for function body */
  return 1.0;
}
